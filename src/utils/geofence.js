// GPS geofence check — confirms a check-in happens within a set radius of
// the training venue's known coordinates, using the browser's Geolocation API.
//
// Built out in the "shared backend" / web app session.

export {};
