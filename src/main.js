import './style.css';

// This file is the entry point — it currently just confirms the scaffold
// runs correctly. Once the backend is set up, this will route between the
// check-in page (pages/checkin.js) and the admin page (pages/admin.js).

document.querySelector('#app').innerHTML = `
  <div class="placeholder">
    <h1>Taekwondo Attendance System</h1>
    <p>Scaffold is running. Next: set up Firebase, then build the check-in flow.</p>
  </div>
`;
